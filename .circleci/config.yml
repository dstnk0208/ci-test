version: 2.1
orbs:
  cypress: cypress-io/cypress@1
workflows:
  build:
    jobs:
      - cypress/run:
          executor: cypress/base-14
          start: npm start & $(npm bin)/wait-on http://localhost:4200 & $(npm bin)/cypress run
          store_artifacts: true